program Hello_World;
var
  toto : integer;
  tata : integer;
  titi : real;
  tutu : char;
  tete : integer;
begin
  tete := 12 * (3 + 1);
  write(13 + 4 * 3);
  toto := -12;
  tata := 4;
  write(12.4 + 2.2);
  write('h');
  write(tata);
  tutu := 's';
  write(tutu);
  tata := toto + 28;
  write(tata);
  toto := toto + tata;
  write(toto);
end.
